name: main
on: push
jobs:
  sast:
    runs-on: ubuntu-latest
      - uses: AppThreat/sast-scan-action@master
        with:
          type: "php"

      - uses: actions/upload-arfifact@v1
        with:
          name: reports
          path: reports